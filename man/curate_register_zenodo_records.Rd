% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zenodo.R
\name{curate_register_zenodo_records}
\alias{curate_register_zenodo_records}
\title{Curate all Zenodo records of a register}
\usage{
curate_register_zenodo_records(
  register_table,
  zenodo = NULL,
  fields = c("title", "publisher", "language", "resource_type", "identifiers", "reviews",
    "repository"),
  dry_run = TRUE,
  register_dir = getwd()
)
}
\arguments{
\item{register_table}{a register `data.frame` with columns `Certificate`,
`Report` and `Repository`}

\item{zenodo}{a `zen4R` ZenodoManager, only needed when `dry_run = FALSE`}

\item{fields}{which classes of correction to consider, see [curate_zenodo_record()]}

\item{dry_run}{if TRUE (the default) only report what would change}

\item{register_dir}{directory holding `register.csv`}
}
\value{
a data.frame with one row per record: `certificate`, `record_id`,
  `applied` (the corrections), `manual` (findings needing a human), `error`
}
\description{
Curate all Zenodo records of a register against the curation policy
}
\details{
Runs [curate_zenodo_record()] over every register entry whose report is a
Zenodo DOI. Intended for the mechanical corrections, whose target values
follow from the certificate ID or from `codecheck.yml`; `fields` therefore
defaults to everything except "creators", which needs a human because
splitting a name is wrong for group entries.

One record failing does not stop the run: its error is recorded in the
result and the loop continues.
}
\author{
Daniel Nuest
}
