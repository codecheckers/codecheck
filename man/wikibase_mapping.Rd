% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wikibase.R
\name{wikibase_mapping}
\alias{wikibase_mapping}
\title{Every entity the instance holds, with its Wikidata counterpart}
\usage{
wikibase_mapping(session = NULL)
}
\arguments{
\item{session}{an optional session; the listing works unauthenticated}
}
\value{
a `data.frame` with columns `local_id`, `wikidata_id` and `label`,
  one row per entity that carries the mapping property (plus the mapping
  property itself, whose `wikidata_id` is `NA`)
}
\description{
Read off the instance rather than from a local file: the instance is the only
authority on what it already contains, and a stale local mapping is exactly
how a bootstrap creates duplicates.
}
\keyword{internal}
