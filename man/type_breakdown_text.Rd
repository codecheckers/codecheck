% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_render_table_codecheckers.R
\name{type_breakdown_text}
\alias{type_breakdown_text}
\title{The hover text shared by the check-type bar and donut}
\usage{
type_breakdown_text(counts, highlight = NULL)
}
\arguments{
\item{counts}{A named integer vector of checks per venue type, already
ordered by [order_type_counts()].}

\item{highlight}{The type to mark, or `NULL` to mark none.}
}
\value{
A single string with `\n` between lines.
}
\description{
Lists *every* type with its count and share, not just the one under the
pointer, so a single hover explains the whole visualisation - which is what
lets the donut (register#207) do without a legend. The type the pointer is
actually over is marked; the others are indented by two spaces so the list
stays aligned.
}
\keyword{internal}
