% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wikibase_export.R
\name{evaluate_model_value}
\alias{evaluate_model_value}
\title{Evaluate one value definition against a register row}
\usage{
evaluate_model_value(value, row, resolve = NULL)
}
\arguments{
\item{value}{a value definition from the model}

\item{row}{a one-row `data.frame` or list of register fields}

\item{resolve}{a function `(entity_kind, key) -> local id or NA`, used for
`entity` values; `NULL` resolves nothing}
}
\value{
a character vector of values, empty when the statement does not apply
}
\description{
Returns zero, one or several values: zero when the row has nothing to say
(the statement is then simply not written, which is how a paper without a
venue gets no `published in`), several when the field holds several - a
certificate with three codecheckers gets three `author` statements.
}
\keyword{internal}
