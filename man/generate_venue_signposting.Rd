% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_build_metadata.R
\name{generate_venue_signposting}
\alias{generate_venue_signposting}
\title{Signposting links for a venue page}
\usage{
generate_venue_signposting(venue_name, venue_type, has_jsonld = FALSE)
}
\arguments{
\item{venue_name}{The venue's name (`venues.csv` `name` column)}

\item{venue_type}{The venue's type, mapped to a Schema.org class by
\code{\link{venue_schema_org_type}}}

\item{has_jsonld}{Whether `index.jsonld` was written next to the page}
}
\value{
HTML string of `<link>` elements, one per line
}
\description{
Venues have persistent identifiers too: `venues.csv` carries a `wikidata`
column, and a Wikidata entity URI is the one PID that exists for every venue
type, which is what `cite-as` needs given its cardinality of 1. The ISSNs in
the `identifiers` column stay where they are, in the Schema.org `sameAs`.
A venue without a Wikidata item simply gets no `cite-as`.
}
