% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wikibase.R
\name{plan_wikibase_entities}
\alias{plan_wikibase_entities}
\title{Plan the entities the CODECHECK Wikibase needs}
\usage{
plan_wikibase_entities(existing)
}
\arguments{
\item{existing}{a mapping as returned by [wikibase_mapping()]}
}
\value{
a `data.frame` with columns `kind` (`"property"` or `"item"`),
  `wikidata_id`, `label`, `description`, `datatype`, `role` and `action`
  (`"create"` or `"present"`)
}
\description{
The pure half of [bootstrap_wikibase()]: given what the instance already
holds, work out what is missing. Separated so the decision can be tested
without touching the network, and so a dry run and a real run cannot disagree
about what would be created.
}
\keyword{internal}
