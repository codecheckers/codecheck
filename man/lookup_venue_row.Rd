% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_render_venue_metadata.R
\name{lookup_venue_row}
\alias{lookup_venue_row}
\title{Look up a venue's row in venues.csv, with an empty fallback}
\usage{
lookup_venue_row(venue_name)
}
\arguments{
\item{venue_name}{The venue's name (register.csv `Venue` column / venues.csv
`name` column value).}
}
\value{
A single-row data frame: the matching venues.csv row if found,
  otherwise a minimal one-column (`name`) data frame.
}
\description{
A venue not (yet) listed in venues.csv still has a type from register.csv,
so callers that only need a type (e.g. the metadata panel, or its JSON
representation) should still get something to work with rather than
skipping the venue entirely.
}
\keyword{internal}
