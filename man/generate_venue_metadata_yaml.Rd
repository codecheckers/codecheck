% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_render_venue_metadata.R
\name{generate_venue_metadata_yaml}
\alias{generate_venue_metadata_yaml}
\title{Generate the venue metadata YAML frontmatter block for register.md}
\usage{
generate_venue_metadata_yaml(venue_row, venue_type = NULL)
}
\arguments{
\item{venue_row}{A single-row data frame from CONFIG$VENUE_DATA (i.e. one
row of venues.csv).}

\item{venue_type}{See [get_venue_metadata_fields()]. `NULL`/`NA` omits the field.}
}
\value{
A YAML string (ending in a newline), or `""` if there is nothing to add.
}
\description{
Renders the same structured venue metadata as [generate_venue_metadata_html()]
and the JSON `venue` field (see [get_venue_metadata_fields()]), but as YAML
lines for register.md's frontmatter header rather than as an HTML block in
the body - register.md is served as a plain markdown/API text file, not
HTML, so embedding an HTML `<div>` in it is wrong (register#84 followup).
}
\keyword{internal}
