% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wikibase_log.R
\name{quickstatements_submitted}
\alias{quickstatements_submitted}
\title{Record that a QuickStatements batch was actually submitted}
\usage{
quickstatements_submitted(
  batch,
  url = NA_character_,
  note = NA_character_,
  file = NULL
)
}
\arguments{
\item{batch}{the batch name given to [quickstatements_write()]}

\item{url}{the QuickStatements batch URL, if there is one}

\item{note}{anything worth recording, e.g. which commands failed}

\item{file}{the log path, or `NULL` to use the option}
}
\value{
the log entry, invisibly
}
\description{
The one thing the code cannot observe, and the one thing worth knowing later:
that a prepared batch was pasted, by whom it was run and where its result can
be seen. QuickStatements gives every run a batch URL; that is what belongs
here.
}
\details{
Recording a batch also retires the `.qs` file it was pasted from, renaming it
with a `.submitted` suffix so that a later run cannot paste the same creates
a second time.
}
\examples{
\dontrun{
quickstatements_submitted("certificates-2026-09", url = "https://...batch/12345")
}
}
