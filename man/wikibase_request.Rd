% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wikibase.R
\name{wikibase_request}
\alias{wikibase_request}
\title{One Action API request, repeated while the server asks us to come back}
\usage{
wikibase_request(
  method,
  params,
  handle = NULL,
  what = "the API",
  attempts = 4,
  api = WIKIBASE_INSTANCE$api
)
}
\arguments{
\item{method}{`httr::GET` or `httr::POST`}

\item{params}{the request parameters}

\item{handle}{an `httr` handle, or `NULL` for an anonymous request}

\item{what}{what is being requested, for messages}

\item{attempts}{how many times to try in total}

\item{api}{the Action API to address, the CODECHECK Wikibase by default}
}
\value{
the parsed response
}
\description{
The single place both reads and writes go through, so the User-Agent, the
retry rule and the JSON parsing are decided once.
}
\keyword{internal}
