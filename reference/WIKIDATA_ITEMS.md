# The Wikidata items the model refers to by name

Constants rather than lookups: these identify the classes the
certificates are typed against, and a rename on Wikidata must not
silently change what the export writes.

## Usage

``` r
WIKIDATA_ITEMS
```
