# External Library Specifications

Single source of truth for the CSS/JavaScript libraries that are stored
locally in the register output, including their font files. Used both
for downloading the libraries and for checking whether the local copies
are already up to date.

## Usage

``` r
external_library_specs()
```

## Value

A named list of library specifications
