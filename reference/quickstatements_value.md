# Render one value as QuickStatements v1 writes it

Render one value as QuickStatements v1 writes it

## Usage

``` r
quickstatements_value(datatype, value)
```

## Arguments

- datatype:

  the property's Wikibase datatype

- value:

  the value

## Value

the value as a QuickStatements token
