# The Assets Directory Belonging to a Libraries Directory

Both live in the render output directory, so "docs/libs" gives
"docs/assets".

## Usage

``` r
register_assets_dir(libs_dir)
```

## Arguments

- libs_dir:

  Base libraries directory

## Value

the path of the assets directory
