# The CODECHECK Wikibase instance

Deliberately a time-limited platform for testing the deposition code and
for demonstrating the result before moving to Wikidata proper, so
nothing may live only there: everything the instance holds has to be
reproducible from \`register.csv\` and this model.

## Usage

``` r
WIKIBASE_INSTANCE
```
