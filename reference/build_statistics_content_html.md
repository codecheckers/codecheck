# Build the HTML body content of the statistics dashboard

Build the HTML body content of the statistics dashboard

## Usage

``` r
build_statistics_content_html(stats, base_path = "..")
```

## Arguments

- stats:

  The parsed contents of \`statistics.json\` (a list)

## Value

A single HTML string, safe to embed as a pandoc raw-HTML block
