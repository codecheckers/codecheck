# Name of the QuickStatements batch for an entity kind

This is what a person types when recording a batch they pasted by hand,
so it is named after what the batch contains.

## Usage

``` r
wikidata_batch_name(kind)
```

## Arguments

- kind:

  an entity kind

## Value

the batch name, without the \`.qs\` extension
