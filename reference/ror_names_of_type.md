# The other names a ROR record carries

The other names a ROR record carries

## Usage

``` r
ror_names_of_type(record, types)
```

## Arguments

- record:

  A ROR record, or \`NULL\`

- types:

  The name types to collect

## Value

A character vector of names, without the display name
