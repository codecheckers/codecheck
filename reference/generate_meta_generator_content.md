# Generate Meta Generator Content

Creates the content value for the HTML meta generator tag with build
information.

## Usage

``` r
generate_meta_generator_content(metadata)
```

## Arguments

- metadata:

  Build metadata from get_build_metadata()

## Value

String with generator content (without HTML tags)
