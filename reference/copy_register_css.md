# Copy the CODECHECK Register CSS to the Output Assets Directory

Copy the CODECHECK Register CSS to the Output Assets Directory

## Usage

``` r
copy_register_css(assets_dir)
```

## Arguments

- assets_dir:

  Directory for the register's own assets, by convention a sibling of
  the libraries directory, see \[register_assets_dir()\]
