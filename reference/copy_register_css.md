# Copy the CODECHECK Register CSS to the Output Assets Directory

Copy the CODECHECK Register CSS to the Output Assets Directory

## Usage

``` r
copy_register_css(assets_dir = "docs/assets")
```

## Arguments

- assets_dir:

  Directory for the register's own assets
