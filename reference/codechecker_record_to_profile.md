# Turn one row of a codechecker list into a profile

Turn one row of a codechecker list into a profile

## Usage

``` r
codechecker_record_to_profile(codechecker)
```

## Arguments

- codechecker:

  One row of \[all_codechecker_records()\].

## Value

A profile list, see \[get_codechecker_profile()\].
