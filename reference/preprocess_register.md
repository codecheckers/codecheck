# Function for preprocessing the register to create and return the preprocessed register table.

Function for preprocessing the register to create and return the
preprocessed register table.

## Usage

``` r
preprocess_register(register, filter_by)
```

## Arguments

- register:

  The register.

- filter_by:

  The filter (if applicable).

## Value

The preprocessed register table
