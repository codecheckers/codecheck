# The title of an OSF project node

The title of an OSF project node

## Usage

``` r
get_osf_record_title(report_link)
```

## Arguments

- report_link:

  An OSF project URL, whose last segment is the node ID

## Value

The node title, or \`NULL\` if the node carries none
