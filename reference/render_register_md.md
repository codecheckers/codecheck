# Renders register md for a single register_table

Renders register md for a single register_table

## Usage

``` r
render_register_md(register_table, table_details, filter)
```

## Arguments

- register_table:

  The register table

- table_details:

  List containing details such as the table name, subcat name.

- filter:

  The filter
