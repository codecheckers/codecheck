# Map a CODECHECK venue type to the closest Schema.org entity type

Map a CODECHECK venue type to the closest Schema.org entity type

## Usage

``` r
venue_schema_org_type(venue_type)
```

## Arguments

- venue_type:

  journal/conference/community/institution (register.csv \`Type\`)

## Value

A Schema.org \`@type\` string
