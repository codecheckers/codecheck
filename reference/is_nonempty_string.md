# Is this a usable single non-empty string?

Is this a usable single non-empty string?

## Usage

``` r
is_nonempty_string(x)
```

## Arguments

- x:

  A value from a parsed codecheck.yml, which may be NULL, NA or ""

## Value

TRUE if \`x\` can be rendered into a meta tag
