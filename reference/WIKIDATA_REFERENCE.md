# A reference block attached to every statement the export writes

Not decoration: it is what distinguishes a statement this pipeline wrote
from one a person added by hand, and it gives a reviewer the page to
check it against. \`S\`-prefixed properties are the QuickStatements
spelling of a reference.

## Usage

``` r
WIKIDATA_REFERENCE
```
